pipeline:
    build:
        group: build
        image: node:9
        commands:
            - make install
            - make build
        when:
            branch: [develop, master]
    deploy-testnet:
        group: deploy
        image: plugins/s3
        acl: public-read
        region: "us-east-1"
        bucket: "wallet.testnet.kowala.io"
        source: dist/**/*
        strip_prefix: dist
        target: /
        encryption: AES256
        when:
            branch: [develop]
